# 模型路由（第 6 周）：别名映射 + 失败降级链
# 请求 model 可为别名（如 chat-fast）或上游真实模型名。

aliases:
  chat-fast: gpt-4o-mini
  chat-strong: gpt-4o

# 主模型失败（429/5xx/网络错误）时按顺序尝试；链首须与 resolve 后的主模型一致。
fallback_chains:
  gpt-4o-mini:
    - gpt-4o-mini
  gpt-4o:
    - gpt-4o
    - gpt-4o-mini
  default:
    - gpt-4o-mini
