---
description: 强制 Issue → feature branch → PR 协作流程，禁止直推 main
alwaysApply: true
---

# Issue 驱动开发（强制执行）

本仓库 **必须** 遵循 [CONTRIBUTING.md](../../CONTRIBUTING.md) §1。无例外（含 AI Agent、维护者本地开发）。

## 禁止

- ❌ 在 `main` 上直接 commit / push 功能、修复、重构
- ❌ 无 GitHub Issue 编号即开始写业务代码
- ❌ 一个巨型 commit 混多个 Issue
- ❌ 跳过 PR 直接合并（除维护者文档 typo 等极小改动）

## 必须顺序

```
docs/roadmap 或 backlog 规划
    → GitHub Issue（含验收标准）
    → git checkout -b feat/issue-<N>-<short-name>
    → 实现 + 单测 + 文档
    → commit（message 含 refs/closes #N）
    → push + 开 PR（填 PR 模板，关联 Issue）
    → CI 绿 + review
    → merge 到 main
    → Phase 完成后打 tag
```

## Agent 执行检查清单

开始编码前：

1. 确认目标 Issue 已存在；若无则 **先** `gh issue create`，再开发
2. `git checkout main && git pull` 后从最新 main 建分支
3. 范围仅覆盖该 Issue，不夹带无关改动

提交前：

1. `ruff check .` / 相关 `pytest` 或 `python -m unittest` 通过
2. PR 描述写 `closes #N`
3. **不要** `git push origin main`

Issue / Milestone / Label 纪律（Phase 工作必做）：

1. **Issue 必须先存在**：编号 `#N` 与 `docs/issues-backlog-phase-*.md` 条目一致
2. **Milestone**：挂到对应 Phase（如 `Phase O — Agent JD2 Alignment`）
3. **Labels**：至少 `phase-task` + `phase-<letter>`（如 `phase-o`）；缺 label 时先 `gh label create` 再建 Issue
4. **分支名**：`feat/issue-<N>-<short-name>` 或 `fix/issue-<N>-<short-name>`（与 Issue 号一致）
5. **Commit / PR**：message 或 body 含 `closes #N`（一个 PR 关闭一个 Issue）
6. **合并后**：确认 Issue 自动关闭；同步更新 `docs/issues-backlog-phase-*.md` 的 PR 列（如 `✅ #99`）
7. **Phase Tag**：该 Phase **全部** Issue merge 后，在 `main` HEAD 打 `phase-<letter>-<name>` 并 `git push origin <tag>`；**禁止**在 feature branch 或未完成 Phase 中间打 tag

Phase 多 Issue 时：优先 **堆叠 PR**（#N 合并后再开 #N+1 分支），或按模块拆独立 PR，每个 PR 对应一个 Issue。
