---
description: 强制 Issue → feature branch → PR 协作流程，禁止直推 main
alwaysApply: true
---

# Issue 驱动开发（强制执行）

本仓库 **必须** 遵循 [CONTRIBUTING.md](../../CONTRIBUTING.md) §1。无例外（含 AI Agent、维护者本地开发）。

## 禁止

- ❌ 在 `main` 上直接 commit / push 功能、修复、重构
- ❌ 无 GitHub Issue 编号即开始写业务代码
- ❌ 一个巨型 commit 混多个 Issue
- ❌ 跳过 PR 直接合并（除维护者文档 typo 等极小改动）

## 必须顺序

```
docs/roadmap 或 backlog 规划
    → GitHub Issue（含验收标准）
    → git checkout -b feat/issue-<N>-<short-name>
    → 实现 + 单测 + 文档
    → commit（message 含 refs/closes #N）
    → push + 开 PR（填 PR 模板，关联 Issue）
    → CI 绿 + review
    → merge 到 main
    → Phase 完成后打 tag
```

## Agent 执行检查清单

开始编码前：

1. 确认目标 Issue 已存在；若无则 **先** `gh issue create`，再开发
2. `git checkout main && git pull` 后从最新 main 建分支
3. 范围仅覆盖该 Issue，不夹带无关改动

提交前：

1. `ruff check .` / 相关 `pytest` 通过
2. PR 描述写 `closes #N`
3. **不要** `git push origin main`

Phase 多 Issue 时：优先 **堆叠 PR**（#N 合并后再开 #N+1 分支），或按模块拆独立 PR，每个 PR 对应一个 Issue。
