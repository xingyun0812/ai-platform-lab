---
description: Agent 模块写法 — self_evolve.py 风格（说明+分区注释+fail-open）
globs: packages/**/*.py
# alwaysApply: false — 仅编辑 packages/**/*.py 时注入，避免污染前端/配置/docs 会话
alwaysApply: false
---

# Agent module style（参考 `self_evolve.py`）

## 1. 文件头 Docstring

```python
"""packages/<module>/<filename>.py — Phase <X> <简短描述>。

<空行>
<可选：核心流程/数据流，分点列出，单行一条>
"""
```

- 第一行：模块路径 + Phase 标识 + 一句话用途
- 可选第二段：核心流程分点罗列（>2 点时用）

示例见 `packages/agent/self_evolve.py` 开头。

## 2. Import 顺序

```python
from __future__ import annotations  # 第一行 import

import json
import logging
import os
import threading
import time
import uuid
from dataclasses import dataclass, field
from typing import Any
```

1. `from __future__ import annotations`（所有新文件必须）
2. 标准库 import（`import xxx`、`from xxx import yyy`，按字母序）
3. 第三方库 import（隔一个空行）
4. 本包 import（`from packages.<module> import ...`）

## 3. Logger 声明

```python
logger = logging.getLogger("ai_platform.<module>.<submodule>")
```

紧跟在 import 块之后，模块级常量之前。

## 4. 分区注释

```python
# ---------------------------------------------------------------------------
# <分区标题：简短>（~50个字符以内）
# ---------------------------------------------------------------------------


@dataclass
class X:
    ...
```

- 75 个 `-`（等号栏线），两边各一个空格
- 每个逻辑分区之间隔 **一个空行**（不是两个）

常见分区：
- `dataclass` 定义
- `XxxStore` 类（InMemory 实现）
- `PostgresXxxStore` 类（Postgres 持久化）
- `全局单例`
- `Core async functions`
- `HITL approve / reject helpers`

## 5. Dataclass 优先

```python
@dataclass
class StrategyPatch:
    """LLM 基于 lessons 提出的策略修改建议。"""

    patch_id: str
    status: str = "pending"  # pending | approved | rejected
    created_at: float = field(default_factory=time.time)
```

- 用 `@dataclass` 而非命名元组或 dict
- 字段注释写类型后面的 `# comment`，而非 docstring
- 可选字段用 `field(default_factory=...)` 或 `= 默认值`

## 6. Fail-open 模式

```python
async def some_llm_call(...) -> str:
    """调用 LLM ...

    失败时回退到 <fallback>，不阻塞主流程。
    """
    try:
        ...
    except Exception as exc:
        logger.warning("some_llm_call failed: %s", exc)
    # 回退逻辑
    return "<fallback_value>"
```

- 任何 LLM 调用、外部服务调用必须 `try/except`
- log 用 `logger.warning`（不是 `error`），因为 fallback 存在
- 始终返回有意义的值（None、空字符串、默认值），不重新 raise

## 7. 全局单例模式

```python
_store: StrategyPatchStore | None = None
_store_lock = threading.Lock()


def get_store() -> StrategyPatchStore:
    global _store
    if _store is None:
        with _store_lock:
            if _store is None:
                ...  # 创建实例
    return _store


def reset_store_for_tests() -> None:
    global _store
    with _store_lock:
        _store = None
```

- 模块级 `_xxx` 变量 + `threading.Lock`
- 双检锁（double-checked locking）延迟初始化
- 必须有一个 `reset_X_for_tests()` 用于 pytest 隔离

## 8. 类型注解

```python
def list_by_status(self, *, tenant_id: str | None = None) -> list[StrategyPatch]:
```

- 所有公开函数必须带类型注解
- 用 `X | None`（Python 3.11+ 语法）而非 `Optional[X]`
- 用 `list[X]`、`dict[str, X]` 而非 typing.List、typing.Dict

## 9. 其他规则

- 行尾不要空格
- 模块级常量：`_UPPER_SNAKE`（下划线开头表示模块私有）
- 多行函数签名：第一个参数在 `(` 后换行，之后每个参数一行并与第一个对齐
- 字符串：业务逻辑用中文，技术引用/URL 用英文
